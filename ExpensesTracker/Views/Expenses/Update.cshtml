@model List<ExpenseModel>
<h2 class="text-center">Update Expenses </h2>
<table class="table table-bordered table-dark text-center">
    <thead>
        <tr>
            <th>Expense</th>
            <th>Amount</th>
            <th>Spent On</th>
            <th>Operation</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model.OrderBy(u => u.ET_Date))
        {
            <tr>
                <td>@obj.ET_Expense_Name</td>
                <td>@obj.ET_Amount</td>
                <td>@obj.ET_Date</td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Expenses" asp-action="Edit" asp-route-id="@obj.ET_Id" class="btn btn-info mx-2">
                            <i class="bi bi-pencil-square"></i>Update
                        </a>
                    </div>
                </td>

            </tr>
        }
    </tbody>
</table>